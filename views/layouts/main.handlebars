<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Link to Bootstrap's CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <title>hackergrove</title>
</head>

<body>
    <header class="container-fluid p-3">
        <div class="row">
            <div class="col-12 col-md-4">
                <h1>HackerGrove</h1>
            </div>
            <div class="col-12 col-md-2 ms-auto">
                {{#if logged_in}}
                <button id="logout" class="btn btn-lg">
                    <h5>Logout</h5>
                </button>
                {{else}}
                <a class="btn btn-lg" href="./login" role="button" id="loginPgBtn">
                    <h5>Login</h5>
                </a>
                {{/if}}
            </div>
        </div>
    </header>
    <main class="m-2">
        {{{body}}}
    </main>
    <footer class="fixed-bottom">
        <div class="container-fluid text-center">
            <div class="row">
                <div class="col-12 col-sm-8 col-lg-6">
                    <h6>Contact us!</h6>
                    <p>
                        <a class="gitLink">Bryan</a>, <a class="gitLink">Elise</a>, <a class="gitLink">Pak</a>
                    </p>
                </div>
            </div>
        </div>
    </footer>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>